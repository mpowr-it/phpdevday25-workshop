# Static Secret: mirrors the shape used in later ESO examples.
# NOTE: Using stringData for readability; the API server converts to base64 in .data.
---
apiVersion: v1
kind: Secret
metadata:
  name: zero-pg-app
  namespace: int-app-zero
  labels:
    # Optional: if you later add Stakater Reloader
    reloader.stakater.com/auto: "true"
    # Optional: CNPG can react on password change (even in static scenario)
    cnpg.io/reload: "true"
type: Opaque
stringData:
  username: "app"
  password: "S3cur3-Pa55w0rd!"
  database: "app"
  host: "zero-pg-rw.int-app-zero.svc.cluster.local"
  port: "5432"
  uri: "postgresql://app:S3cur3-Pa55w0rd!@zero-pg-rw.int-app-zero.svc.cluster.local:5432/app"
  jdbc-uri: "jdbc:postgresql://zero-pg-rw.int-app-zero.svc.cluster.local:5432/app?user=app&password=S3cur3-Pa55w0rd!"

